<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#1a1a1a">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="16dp">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_marginBottom="8dp">
            <Button android:id="@+id/btnPower"
                android:layout_width="90dp" android:layout_height="60dp"
                android:text="POWER" android:backgroundTint="#cc0000"
                android:textColor="#fff" android:layout_marginEnd="8dp"/>
            <Button android:id="@+id/btnInput"
                android:layout_width="90dp" android:layout_height="60dp"
                android:text="Input" android:backgroundTint="#444"
                android:textColor="#fff" android:layout_marginEnd="8dp"/>
            <Button android:id="@+id/btnMute"
                android:layout_width="90dp" android:layout_height="60dp"
                android:text="Mute"
                </ScrollView>
